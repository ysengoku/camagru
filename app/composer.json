{
  "name": "camagru/project",
  "description": "Camagru application",
  "type": "project",
  "require": {},
  "require-dev": {
    "squizlabs/php_codesniffer": "^3.7"
  },
  "scripts": {
    "lint": "vendor/bin/phpcs --standard=phpcs.xml src app",
    "fix": "vendor/bin/phpcbf --standard=phpcs.xml src app"
  },
  "config": {
    "preferred-install": "dist"
  }
}
